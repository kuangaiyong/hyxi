[pytest]
# 钉住 rootdir = 仓库根。没有配置文件时 pytest 按命令行里「像路径的参数」的公共祖先算 rootdir，
# 参数里混进一个仓库外的路径（比如临时的覆盖率配置）就会漂到盘符根：nodeid 变成 code/hyxi/backend/…，
# --deselect、按用例记的覆盖率上下文、选测映射就全对不上了（实测）。
# 会话级护栏（临时数据目录、browser 标记、并行分组）在 backend/tests/conftest.py
testpaths = backend/tests
